{% extends 'base.html.twig' %}

{% block title %}New Employee
{% endblock %}

{% block body %}
	<h1>Create new Employee</h1>

	<div class="p-4">
		{{ include('employee/form.html.twig') }}
	</div>
	<div class="col-md-12 d-flex justify-content-center gap-2 mb-1">
		<a class="btn btn-info" href="{{ path('app_employee_index') }}">back to list</a>
	</div>

{% endblock %}
